import "Std/List" 
type Response = /[Int Int /Int]

def call [l:Int r:Int f:Response] = printi!(f l r)

def add (x:Int y:Int):Int = (+ x y)
def sub (x:Int y:Int):Int = (- x y)

run call![5 3 add]
run call![3 5 add]
run call![5 3 sub]
run call![3 5 sub]